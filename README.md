# cifar10-pytorch-classification
CNN-based image classification project implemented in PyTorch. The model was trained and evaluated on CIFAR-10-like dataset.
# CIFAR-10 Image Classification (PyTorch)

This project implements an image classification model using a Convolutional Neural Network (CNN) in PyTorch.

## 📌 Project Details
- Framework: PyTorch
- Task: Image Classification
- Dataset: CIFAR-10-like (FakeData used due to download issues)
- Model: CNN

## 🚀 Results
The model was successfully trained and tested, producing classification outputs.

## ▶️ How to Run
Open the notebook in Google Colab and run all cells.
